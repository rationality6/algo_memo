#include <stdio.h>

int min(int a, int b, int c) {
        a = a > b ? b : a;
        return a < c ? a : c;
}

int main(){
        int i, j, k=0,n,m,b[1001][1001],d[1001];
        
        return 0;
}
